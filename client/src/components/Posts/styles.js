import styled from "styled-components";
import {Grid} from '@mui/material'

export const Container = styled(Grid)`
    display: flex;
    align-items: center;
  .smMargin {
    margin: 10px;
  }
  .actionDiv{
    text-align: center;
  }
`